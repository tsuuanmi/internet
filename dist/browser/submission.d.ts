import type { Locator } from "patchright-core";
/** Wait for a provider's send control to settle and become enabled after input. */
export declare function waitForSendReady(provider: string, button: Locator, timeoutMs?: number): Promise<void>;
//# sourceMappingURL=submission.d.ts.map